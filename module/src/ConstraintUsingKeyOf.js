"use strict";
{
    const getPropratyValues = (obj, key) => {
        return obj[key];
    };
    const user = {
        name: 'jahid',
        age: 87,
        address: "ctg"
    };
    const result = getPropratyValues(user, "name");
}
